use System as test;
use Math, Assert;
include testresources.core.keywords.includes.IncludePackageConstructs as Math;
include testresources.core.keywords.includes.IncludePackageConstructs as test;

Assert.equal(Math.round(5.3), 5);
Assert.isNull(test.Print("Test"));